const path = require('path');

module.exports = {
  config: path.resolve('configuration/db.config.js'), 
  'models-path': path.resolve('models/'),            
  'migrations-path': path.resolve('migrations/')
};
